import Section from '../components/Section'
import SubsRepos from '../components/SubsRepos'

function ReposPage() {
  return (
    <Section title="仓库" subtitle="全仓库展示（可在配置里开关）。">
      <SubsRepos />
    </Section>
  )
}

export default ReposPage
